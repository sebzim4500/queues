First `npm install` and `typings install` the necessary libraries. Call `gulp static-files` and then `webpack` to build the project. 

Use `node dist/server/bundle.js` to run the server on port 8080.
